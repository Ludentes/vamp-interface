output/
data/*.json
data/*.npy

# Heavy model artifacts — too large for git
models/flux_sliders/*/slider_*.safetensors
models/flux_sliders/*/training_state_*.pt
models/flux_sliders/*/_cache/
models/flux_sliders/*/train_log.csv
models/flux_sliders/*/args.json
models/blendshape_nmf/*.parquet
models/blendshape_nmf/*.parquet.bak.*
models/arc_distill/*.pt

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / web
# The full web/ tree is a separate project (WIP), not part of this repo
web/
web/node_modules/
web/dist/
web/.vite/
node_modules/
dist/
*.log
npm-debug.log*
pnpm-debug.log*

# Env / secrets
.env
.env.*
!.env.example
*.key
*.pem
credentials.json

# Private infra / runbooks / plans (contain LAN IPs, dev DSNs)
docs/design/comfyui-shard-windows.md
docs/runbooks/telejobs-db.md
v1/plans/superpowers/2026-04-09-v6-clip-interpolation.md

# Agent / session state
.claude/*
!.claude/agents/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Large local artefacts (generated faces, dumps)
*.png
*.jpg
*.jpeg
*.mp4
*.webp
*.npy
*.npz
*.pkl
*.parquet
!docs/**/*.png
!docs/**/*.jpg
web/*
data/*
output/*
# demographic_pc Stage 0
vendor/weights/
vendor/MiVOLO/
vendor/FairFace/
vendor/pytorch-DEX/
docs/research/external/

# vendored research code (not loaded by ComfyUI; reference only, multi-GB)
vendor/FluxSpace/

# generated datasets (regenerable from corpus pipelines)
datasets/

# slider training artifacts (safetensors, eval parquets, sample grids)
models/sliders/
models/flux_sliders/eye_squint_*/
models/flux_sliders/collages/*.log
models/flux_sliders/collages/*.txt

# downloaded models (mediapipe, etc.)
models/mediapipe/

# distillation training artifacts (checkpoints, logs, eval bundles)
models/mediapipe_distill/
models/siglip_distill/

# research artifact bundles (mp4/log/json captured from runs; regenerable)
docs/research/_data/

# blendshape NMF measurement caches and per-run JSON reports
models/blendshape_nmf/attn_cache/
models/blendshape_nmf/*.json
models/blendshape_nmf/*.txt

# research-doc supporting images (regenerable from scripts; covers stay tracked)
docs/research/images/

# FFHQ shard-0 kept locally for repeated smoke runs (~458 MB, gitignored;
# covered by *.parquet above but listed explicitly so its purpose is obvious)
tests/fixtures/ffhq_shard0.parquet

# Live Link Face captures — private (real face data) + media files.
data/llf-takes/
data/llf-clips/

# Phase 2 grid renders (PersonaLive output on private driver clips)
data/llf-phase2/*.mp4
data/llf-phase2/grid_manifest.json
